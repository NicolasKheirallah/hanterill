import { EcuReference } from "@/components/docs/mdx/EcuReference";

ECU codes, names and part numbers for the CMA electric platform, grouped as Hanterill lays them out around the gateway. Names and numbers follow published wiring documentation. UDS means the ECU answers a diagnostic session. The same ECU data drives the topology diagram on the home page.

## Gateway

<EcuReference domain="gateway" />

## Energy

<EcuReference domain="energy" />

## Chassis

<EcuReference domain="chassis" />

## Cabin

<EcuReference domain="cabin" caption="Blank part numbers are positions the public documentation does not list. Hanterill still reads identification from the ECU directly with the ReadDataByIdentifier service." />

## Common identifiers

<SpecList rows={[
  ["0xF190", "VIN"],
  ["0xF18C", "ECU serial number"],
  ["0xF193", "Supplier hardware version"],
  ["0xF194", "Supplier software number"],
  ["0xF195", "Supplier software version"],
]} />

Hanterill reads these from every ECU that answers, not just the battery module, so a full scan produces a hardware and software inventory of the car.
