import { PlatformSupport } from "@/components/docs/mdx/PlatformSupport";
import { VehicleSupport } from "@/components/docs/mdx/VehicleSupport";
import { Wip } from "@/components/docs/mdx/Wip";
import { Experimental } from "@/components/docs/mdx/Experimental";

openCMA is built CMA-first. The Compact Modular Architecture platform under Polestar 2 and the smaller electric Volvos is where battery, ECU and routine coverage was written and validated.

## Platform compatibility is not support

Two different things:

- **Platform compatibility** means openCMA can reach the vehicle over DoIP.
- **Verified support** means the diagnostic functions are implemented and checked
  against known-good references.

A car can be reachable and still expose almost nothing useful. Status labels on
this site always reflect verified support.

## Status labels

| Label | Meaning |
| --- | --- |
| Supported | Verified runtime implementation. Battery, DTC, ECU identification and live data are exercised regularly. |
| Partial | Some functions verified, others not yet covered on this vehicle. |
| Testing | Implementation exists but needs validation. |
| WIP | Actively being developed. Connects, but decoding is incomplete. |
| Research | Architecture being investigated. Not usable for diagnostics. |
| Planned | On the roadmap. No implementation yet. |

## Platforms

The tables below are generated from the same support data the rest of the site
uses, so they cannot drift from the vehicles page.

### CMA

<PlatformSupport platform="CMA" />

The full capability breakdown for the reference vehicle:

<VehicleSupport model="Polestar 2" />

### SPA

<Wip note="First-generation Recharge plug-in hybrids. openCMA connects over DoIP; ECU mapping and battery decoding are incomplete. Do not rely on any figures from these vehicles." />

<PlatformSupport platform="SPA" />

### SEA

<Experimental kind="research" note="Newer BEVs including Zeekr. Battery and ECU layout differ from CMA. Sub-platform names such as SEA1 or SEA2 are not shown until a mapping is confirmed." />

<PlatformSupport platform="SEA" />

### SPA2

<Experimental kind="research" />

<PlatformSupport platform="SPA2" />

## Not manufacturer support

Compatibility describes which vehicles openCMA can communicate with. It is not a claim of endorsement. openCMA is an independent project and is not affiliated with Volvo Cars, Polestar or Geely.
